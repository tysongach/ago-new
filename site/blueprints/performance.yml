title: Performance
pages: false
fields:
  title:
    label: Title
    type: text
  venue:
    label: Venue
    type: select
    options: query
    query:
      page: ../../venues
      fetch: visibleChildren
  performers:
    label: Performers
    type: structure
    default: # currently required due to bug
      - performer: jerry-seinfeld
    entry: >
      {{performer}}
    fields:
      performer:
        label: Performer
        type: select
        options: query
        query:
          page: ../../performers
          fetch: visibleChildren
